module eu.stefaner.elasticlists.data {	import MapExtent = com.modestmaps.core.MapExtent;	/**	 * @author mo	 */	export class GeoConstraint extends FilterConstraint {		private facet : GeoFacet;		private filterExtent : MapExtent;		constructor(facet : GeoFacet, filterExtent : MapExtent) {			this.facet = facet;			this.filterExtent = filterExtent;		}		/*override*/ public match(c : ContentItem) : boolean {			for (var facetValue:any in c.facetValues) {				if(facetValue instanceof this.GeoFacetValue) {					if(this.filterExtent.contains((<GeoFacetValue>facetValue ).location)) {						return true;					}				}			}			return false;		}	}}